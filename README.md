# IphoxOS Kernel Pack

This pack introduces the first technical Kernel structure for IphoxOS.

## Install

Copy these folders into the root of the `IphoxOS` GitHub repository:

- `kernel/`
- `docs/`

Then commit with:

```text
Genesis: Create the IphoxOS Kernel.
```
